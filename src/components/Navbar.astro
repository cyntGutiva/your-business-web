<nav class="fixed w-full z-50 bg-slate-950/80 backdrop-blur-md border-b border-slate-800">
    <div class="max-w-7xl mx-auto px-4 md:px-6 py-4 flex justify-between items-center gap-4">
        <div class="flex items-center gap-3 md:gap-4 shrink-0">
            <img src="/logo.png" alt="Logo" class="w-8 h-8 md:w-10 md:h-10 object-contain" />
            
            <div class="text-xl md:text-2xl font-bold text-cyan-400 tracking-tight">
                YourBusinessWeb
            </div>
        </div>

        <div class="hidden md:flex space-x-8 font-medium text-slate-400">
            <a href="#proceso" class="hover:text-cyan-400 transition">Cómo funciona</a>
            <a href="#precios" class="hover:text-cyan-400 transition">Precios</a>
            <a href="#contacto" class="hover:text-cyan-400 transition">Contacto</a>
        </div>

        <div class="flex items-center gap-4">
            <a href="#contacto" class="hidden sm:block bg-cyan-500 text-slate-950 px-5 py-2 rounded-full font-semibold hover:bg-cyan-400 transition text-sm">
                Comenzar
            </a>

            <button id="menu-btn" class="md:hidden text-slate-300 focus:outline-none">
                <svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7"></path>
                </svg>
            </button>
        </div>
    </div>

    <div id="mobile-menu" class="hidden md:hidden bg-slate-900 border-b border-slate-800 absolute w-full left-0 p-6 flex flex-col space-y-4 shadow-2xl">
        <a href="#proceso" class="text-slate-300 hover:text-cyan-400 text-lg py-2 border-b border-slate-800/50">Cómo funciona</a>
        <a href="#precios" class="text-slate-300 hover:text-cyan-400 text-lg py-2 border-b border-slate-800/50">Precios</a>
        <a href="#contacto" class="text-slate-300 hover:text-cyan-400 text-lg py-2">Contacto</a>
        <a href="#contacto" class="bg-cyan-500 text-slate-950 text-center py-3 rounded-xl font-bold">Comenzar proyecto</a>
    </div>
</nav>


<script is:inline>
  const menuBtn = document.getElementById('menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');

  menuBtn.addEventListener('click', () => {
    mobileMenu.classList.toggle('hidden');
  });

  // Cerrar el menú automáticamente al hacer clic en un link
  const navLinks = mobileMenu.querySelectorAll('a');
  navLinks.forEach(link => {
    link.addEventListener('click', () => {
      mobileMenu.classList.add('hidden');
    });
  });
</script>